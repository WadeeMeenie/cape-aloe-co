# Public Supabase project URL. Safe to expose in the browser.
VITE_SUPABASE_URL=https://vnitwsjidlurlwlpsmtf.supabase.co
# Supabase publishable/anon key. Safe for browser use when RLS/security rules are configured.
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key

# Yoco secret stays in Supabase Edge Function secrets and is NEVER placed in VITE_ variables.
# The shared Lekka Supabase project already has YOCO_SECRET_KEY configured.
